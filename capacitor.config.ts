import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.birdFoot',
  appName: 'birdFoot',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
